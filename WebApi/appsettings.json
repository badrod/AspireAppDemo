{
  "OpenAISettings": {
    "Endpoint": "https://api.openai.com/v1",
    "DeploymentName": "gpt-5"

  },
  "AzureOpenAISettings": {
    "Endpoint": "https://your-azure-openai-endpoint.openai.azure.com/",
    "DeploymentName": "your-deployment-name",
    "ApiKey": "your-azure-openai-api-key"
  },
  "AzureSettings": {
    "StorageAccount": "yourstorageaccount",
    "KeyVaultUrl": "https://yourvault.vault.azure.net/"
  },

  // Feature flags to enable or disable specific features
  // Should be stored in Azure App Configuration and will work without restarting the app
  "FeatureManagement": {
    "EnableNewFeature": true 
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
/*
Keys should be stored using KeyVault or the Secret Manager tool for development purposes:
Navigate to the project folder and run the following commands:
dotnet user-secrets init
dotnet user-secrets set "OpenAISettings:ApiKey" "your-secret-key"
*/
